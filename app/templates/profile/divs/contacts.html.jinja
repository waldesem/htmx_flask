{% from "/profile/macros/divs/contacts.html.jinja" import contact_div_macro %}

{% set id = id | default(0) %}
{% set contacts = items | default([]) %}
{{ contact_div_macro(id, contacts = contacts) }}
